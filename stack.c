#include "monty.h"
/**
 * o_stack - prints the top
 * @head: stack head
 * @counter: line_number
 * Return: no return
*/
void o_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
